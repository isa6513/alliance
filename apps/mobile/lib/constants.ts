/** Height of the KeyboardExtender bar (Add photos / Post) in EditableContentForm. Use as bottomOffset for KeyboardAwareScrollView when that form is focused. */
export const KEYBOARD_EXTENDER_BAR_HEIGHT = 60;

/** Extra bottomOffset for KeyboardAwareScrollView when the screen has a bottom tab bar. */
export const KEYBOARD_BOTTOM_OFFSET_WITH_TAB_BAR = 72;

/** Smaller bottomOffset for KeyboardAwareScrollView (e.g. contract screen). */
export const KEYBOARD_BOTTOM_OFFSET_COMPACT = 50;
